{
  "name": "@medikiosk/ai",
  "version": "0.1.0",
  "private": true,
  "description": "Pure provider abstractions for ASR, TTS, OCR and LLM with deterministic mocks. No network/IO, no timers. The interview engine never depends on a speech or model vendor (ASR-independent).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@medikiosk/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}