{
  "name": "@medikiosk/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Password hashing, JWT issuance/verification, RBAC role definitions and permission checks.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@medikiosk/shared-types": "*",
    "bcryptjs": "^2.4.3",
    "jose": "^5.9.6",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.7.2"
  }
}
