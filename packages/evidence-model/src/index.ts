/**
 * @medikiosk/evidence-model
 *
 * Evidence provenance graph and deterministic contradiction detection. Pure, DB-free, LLM-free:
 * assembly and detection are pure functions; the detectors never decide which source is correct.
 */

export * from "./graph";
export * from "./contradictions";
